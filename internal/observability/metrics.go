package observability

// Metrics is an alias for IncidentMetrics to provide a simpler type name
// when passing metric bundles around (for example into storage functions).
type Metrics = IncidentMetrics

